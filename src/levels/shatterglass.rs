//! Campaign level 9 — **Shatterglass Vein** (`ThemeId::Crystal`).
//!
//! A radiant crystal mine of glassy magenta veins in near-black rock.
//! PLACEHOLDER: this map is the shared [`stub_level`](super::stub_level) skinned
//! in the Crystal theme so the campaign boots; a bespoke layout lands in a later
//! pass.

use crate::level::Build;

pub fn build(b: &mut Build) {
    super::stub_level(b);
}
